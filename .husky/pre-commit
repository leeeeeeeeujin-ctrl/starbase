@echo off
REM Windows-friendly Git hook that delegates to the Node script.
REM This file will run when Git executes hooks via cmd/PowerShell on Windows.

SETLOCAL ENABLEDELAYEDEXPANSION
node "%~dp0pre-commit.js" %*
EXIT /B %ERRORLEVEL%
